{
  "name": "@tinymce/oxide-components",
  "tags": [
    "open-source",
    "library"
  ],
  "targets": {
    "build": {
      "outputs": [
        "{projectRoot}/dist",
        "{projectRoot}/lib",
        "{projectRoot}/storybook-static"
      ]
    },
    "node-test": {
      "inputs": [
        "tiny-library",
        "{projectRoot}/vitest.config.ts"
      ],
      "dependsOn": [ "^build" ]
    }
  }
}
