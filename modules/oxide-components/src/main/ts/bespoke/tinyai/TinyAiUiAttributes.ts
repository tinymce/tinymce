const aiSpinner = 'tox-ai__spinner';

const classArrayToClass = (classes: string[]): string => classes.join(' ');

const classes = {
  aiSpinner: classArrayToClass([ aiSpinner ]),
};

export {
  classes
};
