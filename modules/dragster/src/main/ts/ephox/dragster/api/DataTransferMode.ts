import { getMode, setMode, isInReadWriteMode, isInReadOnlyMode, isInProtectedMode } from '../datatransfer/Mode';

export {
  getMode,
  setMode,
  isInReadWriteMode,
  isInReadOnlyMode,
  isInProtectedMode
};
