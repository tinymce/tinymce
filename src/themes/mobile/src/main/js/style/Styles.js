define(
  'tinymce.themes.mobile.style.Styles',

  [

  ],

  function () {
    var resolve = function (p) {
      return 'tinymce-mobile-' + p;
    };

    return {
      resolve: resolve
    };
  }
);
